#!/usr/bin/env bash
set -euo pipefail
LOG_FILE="${MOCK_LOG:-${BATS_TMPDIR:-/tmp}/curl.log}"
/bin/mkdir -p "$(dirname "$LOG_FILE")" 2>/dev/null || true
echo "curl $*" >> "$LOG_FILE"
# Return empty script that does nothing, to avoid network
echo 'echo "mock curl install script executed"'
exit 0
